[tool.ruff]
line-length = 88
extend-exclude = ["docs", "infra"]
target-version = "py310"

[tool.ruff.lint]
# Activa las reglas equivalentes a flake8 (E/F) + isort (I)
select = ["E", "F", "I"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
